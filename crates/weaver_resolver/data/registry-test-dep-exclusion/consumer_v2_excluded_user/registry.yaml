file_format: definition/2
metrics:
  - name: consumer.also.excluded
    brief: Consumer metric that itself is excluded; may freely reference excluded parent items.
    instrument: counter
    unit: "{request}"
    stability: stable
    annotations:
      dependency_resolution:
        exclude: true
    attributes:
      - ref: parent.excluded
        requirement_level: required
